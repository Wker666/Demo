#request url = var
#define payload = /admin/browse.jsp?JMSDestination=event
#define bug =  401 Unauthorized
#define info = 存在ActiveMQ 反序列化漏洞（CVE-2015-5254）

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[1],bug,0)!= "-1")
	{
		print(url.info);
	}
}